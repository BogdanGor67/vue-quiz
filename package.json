{
  "name": "vue-quiz",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:serve": "json-server --watch db.json --port=3001",
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "all": "npm-run-all --parallel db:serve serve"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "core-js": "^3.6.5",
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "~4.5.0",
    "@vue/cli-service": "~4.5.0",
    "@vue/compiler-sfc": "^3.0.0",
    "json-server": "^0.17.0",
    "npm-run-all": "^4.1.5"
  }
}
